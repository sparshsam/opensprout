# v0.9.11 — MCP Real-World Validation

**Goal:** Prove Hermes can operate OpenSprout like a user. Connect, run all 28 tools, execute the reliability audit, and fix discovered issues.

## Checklist

### 1. Hermes Configuration
- [x] Add `env:` block to config.yaml `mcp_servers.opensprout`
- [x] Restart Hermes gateway (MCP servers connect at agent startup)
- [ ] Verify Hermes sees the `mcp_opensprout_*` tools in the CLI toolset

### 2. Real-World Audit (v0.9.10 findings)

| Test | Result v0.9.10 | Status |
|------|-----------------|--------|
| Add monstera plant | Works | ✅ |
| List all plants | Works | ✅ |
| Create 7-day watering schedule | Works | ✅ |
| Complete watering task | No pending tasks (async generation) | ⚠️ Expected |
| Create journal entry | Fails — healthScore 9, DB constraint is 1-5 | ✅ Fixed in v0.9.10 |
| Archive plant | Works | ✅ |
| Restore plant | Works | ✅ |
| Delete plant | Works | ✅ |

### 3. v0.9.11 New Tools
- [ ] `search_plants` — search by name/location/species (instead of downloading all)
- [ ] `get_upcoming_tasks` — tasks due within N days
- [ ] `export_data` — full JSON export

### 4. Documentation Updates
- [ ] Update `docs/mcp-integration.md` with new tools
- [ ] Update `docs/mcp-agent-prompts.md` with search and export prompts
- [ ] Update `docs/mcp-reliability-audit.md` with v0.9.11 results
- [ ] Add `export_data` limitation to app-only flows section

### 5. Validation
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `npm run test`
- [ ] Run reliability audit script end-to-end
- [ ] All 112+ tests pass

### Known Limitations (still app-only)
- Photos (camera/gallery, cover photo, signed URLs)
- Login / Logout / Password reset
- Data import
- Account management
- Task instances are generated asynchronously (not immediately on schedule creation)
